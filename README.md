### Simple grid with WASD controls.
## Packages required:
* Express
* socket.io

Install packages by opening the terminal in the folder and type `npm install express socket.io` and enter.

**To start** the server, type `node index.js` and **to check**, go to http://localhost:3000 and it connects right away.
